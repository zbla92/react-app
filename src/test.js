import React from 'react';

const App = () => {
    return (
        
        
    );
}

const Segment = (props) => {
    return(
        <div className="ui placeholder segment">
        </div>
    );
}

